import 'package:flutter/material.dart';

/// Centralized color definitions matching the Dribbble design
class AppColors {
  AppColors._();

  // Light green background for Home screen
  static const Color lightGreen = Color(0xFF34F699);

  // Dark background for Practice & Question screens
  static const Color darkBackground = Color(0xFF070707);

  // Accent bright green for selections & buttons
  static const Color accentBlue = Color(0xFF54EBFF);

  static const Color backgroundLight = Colors.black54;

  // Primary text on dark backgrounds
  static const Color textOnDark = Colors.white;

  // Primary text on light backgrounds
  static const Color textOnLight = Colors.black;

  // Secondary text (subheadings, captions)
  static const Color secondaryText = Colors.black54;

  static const Color secondarylightText = Colors.black38;

  // Light grey for inactive icons or placeholders
  static const Color lightGrey = Color(0xFFBBBBBB);

  // Light grey for inactive icons or placeholders
  static const Color cardColor = Color(0xFF1E1E1E);

  // Divider / border color
  static const Color divider = Color(0xFF2A2A2A);
}
